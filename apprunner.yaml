version: 1.0
runtime: python311
build:
  commands:
    build:
      - docker build -t pharma-api -f api/Dockerfile .
run:
  runtime-version: 3.11
  command: cd api && uvicorn main:app --host 0.0.0.0 --port 8000
  network:
    port: 8000
  env:
    - name: ENV
      value: production
